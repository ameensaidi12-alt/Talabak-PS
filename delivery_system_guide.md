# دليل نظام التوصيل الهجين (Hybrid Delivery System)

هذا المستند يشرح الأنظمة المختلفة لحساب تكاليف التوصيل في منصة **HatStar** وكيفية إدارتها.

---

## 1. المفاهيم الأساسية للتسعير

تم تصميم النظام ليكون مرناً جداً بحيث يدعم ثلاثة مستويات من التسعير:

### أ. التسعير الذكي (Smart Pricing) - الـ Default
هو النظام الأساسي الذي يعتمد على **المنطقة الجغرافية**.
- يتم تحديد سعر التوصيل بناءً على "منطقة المتجر" و "منطقة الزبون".
- يتم جلب السعر تلقائياً من جدول `delivery_fees` في قاعدة البيانات.
- **الفائدة:** يضمن عدالة السعر للمسافات البعيدة والقريبة تلقائياً.

### ب. السعر الثابت المخصص (Custom Fixed Fee)
يستخدم عندما لا يرغب المتجر باتباع التسعير الذكي ويفضل وضع سعر توصيل ثابت لأي مكان.
- يتم تفعيله عبر خيار "سعر توصيل ثابت للمتجر" في لوحة الأدمن.
- يلغي السعر الذكي ويفرض المبلغ المحدد (مثلاً 10 ₪ ثابتة للجميع).

### ج. عروض التوصيل المجاني (Free Delivery Threshold)
ميزة تسويقية تسمح للمتجر بتقديم توصيل مجاني للطلبات الكبيرة.
- يحدد الأدمن "الحد الأدنى" (مثلاً 100 ₪).
- إذا وصل الزبون لهذا الحد، تصبح تكلفة التوصيل **صفر**.
- **الميزة الكبيرة:** إذا كان الطلب أقل من الحد، يعود النظام لـ **"التسعير الذكي"** تلقائياً لضمان الحماية السعرية.

---

## 2. كيفية الإعداد في تطبيق الأدمن (AdminStarhat)

تم تنظيم واجهة إعدادات المتجر لتكون واضحة كالتالي:

1.  **قسم "عرض التوصيل المجاني":**
    - ادخل المبلغ (مثلاً 150) في خانة "مجاني للطلبات فوق (₪)".
    - هذا الخيار يعمل مع التسعير الذكي ومع السعر الثابت.
2.  **قسم "سعر التوصيل الثابت":**
    - قم بتفعيل المفتاح (Switch) ليصبح المتجر مستقلاً بتسعيره.
    - ادخل السعر (مثلاً 20 ₪) ليصبح هو السعر المعتمد دائماً.

---

## 3. منطق الحساب في تطبيق الزبون (hatStar Logic)

يتم الحساب برمجياً في ملف `cart_screen.dart` حسب التسلسل التالي (Priority):

```dart
// تسلسل اتخاذ القرار في النظام:
1. هل المتجر مُعلم كـ "توصيل مجاني دائم"؟ -> السعر = 0
2. هل إجمالي الطلب >= "الحد الأدنى للتوصيل المجاني" للمتجر؟ -> السعر = 0
3. هل المتجر لديه "سعر ثابت مخصص" مفعل؟ -> السعر = السعر الثابت
4. (Fallback) إذا لم يتحقق ما سبق -> السعر = "التسعير الذكي" (حسب المنطقة).
```

---

## 4. التجربة البصرية للزبون (UI/UX)

- **في صفحة المتجر:** تظهر شارة خضراء بعبارة "توصيل مجاني فوق 100 ₪" لتحفيز الزبون.
- **في السلة:** تظهر رسوم التوصيل وتتحدث لحظياً عند تغيير الكميات. إذا وصل الزبون للحد المطلوب، يرى كلمة **"مجاني"** فوراً بجانب رسوم التوصيل.
- **في كرت المتجر:** يظهر السعر الذكي المتوقع (مثلاً 15 ₪) وبجانبها ملاحظة "مجاني فوق 100 ₪".

---

## 5. الجانب التقني (Database Schema)

الحقول المسؤولة في جدول `vendors`:
- `use_custom_delivery` (bool): هل السعر الثابت مفعل؟
- `custom_delivery_fee` (numeric): قيمة السعر الثابت.
- `free_delivery_threshold` (numeric): حد التوصيل المجاني.

---
**ملاحظة:** يتم حساب التوصيل بشكل مستقل لكل متجر داخل السلة في حال كان الطلب من عدة متاجر في آن واحد.
